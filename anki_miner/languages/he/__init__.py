"""Hebrew language profile (spec Appendix F.2): no engine, no pack, no pip extra.

The card front comes from the installed Wiktionary dictionary itself -- a niqqud fold plus a
proclitic ladder whose candidates are validated by the R36 form lookup -- so Hebrew needs nothing
downloaded beyond the dictionary a user installs anyway.
"""

from __future__ import annotations

__all__: list[str] = []
